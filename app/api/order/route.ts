import {NextResponse} from "next/server";
import {createClient} from "@supabase/supabase-js";
const db=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);
export async function POST(req:Request){try{const b=await req.json();if(!b?.customer?.name||!b?.customer?.phone||!Array.isArray(b.items)||!b.items.length)return NextResponse.json({error:"Datos incompletos"},{status:400});const ids=b.items.map((x:any)=>x.productId);const {data:products,error}=await db.from("products").select("id,name,price,stock,active").in("id",ids);if(error)throw error;const map=new Map((products||[]).map((p:any)=>[p.id,p]));let total=0;const items:any[]=[];for(const item of b.items){const p:any=map.get(item.productId),qty=Number(item.quantity);if(!p||!p.active||!Number.isInteger(qty)||qty<1||qty>p.stock)return NextResponse.json({error:"Producto sin stock o inválido"},{status:400});const subtotal=Number(p.price)*qty;total+=subtotal;items.push({product_id:p.id,product_name:p.name,unit_price:p.price,quantity:qty,subtotal})}const {data:order,error:orderError}=await db.from("orders").insert({customer_name:b.customer.name,customer_phone:b.customer.phone,customer_email:b.customer.email||null,notes:b.customer.notes||null,status:"pending",total}).select("id,total").single();if(orderError)throw orderError;const {error:itemError}=await db.from("order_items").insert(items.map(i=>({...i,order_id:order.id})));if(itemError)throw itemError;return NextResponse.json({orderId:order.id,total:Number(order.total)})}catch(e){console.error(e);return NextResponse.json({error:"No se pudo registrar el pedido."},{status:500})}}