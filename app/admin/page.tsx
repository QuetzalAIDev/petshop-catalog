"use client";import{useEffect,useState}from"react";import{supabase}from"../../lib/supabase-browser";export default function Admin(){const[s,setS]=useState<any>(),[email,setEmail]=useState(""),[pass,setPass]=useState(""),[error,setError]=useState(""),[products,setProducts]=useState<any[]>([]),[form,setForm]=useState<any>({name:"",description:"",price:"",stock:0,category:"Perros",image_url:"🐾"}),[edit,setEdit]=useState<string|null>(null);const load=async()=>{const p=await supabase.from("products").select("*").order("created_at",{ascending:false});setProducts(p.data||[])};useEffect(()=>{supabase.auth.getSession().then(({data})=>{if(data.session?.user.app_metadata?.role==="admin"){setS(data.session);load()}})},[]);async function login(){const{data,error}=await supabase.auth.signInWithPassword({email,password:pass});if(error||data.user?.app_metadata?.role!=="admin")return setError("Acceso de administrador requerido");setS(data.session);load()}async function save(){const payload={...form,price:Number(form.price),stock:Number(form.stock),slug:form.name.toLowerCase().replace(/[^a-z0-9]+/g,"-")+"-"+Date.now()};edit?await supabase.from("products").update(payload).eq("id",edit):await supabase.from("products").insert(payload);setEdit(null);load()}if(!s)return <main className="admin"><section className="hero"><h1>🔐 Administración</h1></section><div className="panel"><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><br/><br/><input className="input" type="password" placeholder="Contraseña" value={pass} onChange={e=>setPass(e.target.value)}/><br/><br/><button className="primary" onClick={login}>Entrar</button><p>{error}</p></div></main>;return <main className="admin"><div className="row"><h1>🐾 Administración</h1><button className="chip" onClick={()=>supabase.auth.signOut().then(()=>location.reload())}>Salir</button></div><section className="panel"><h2>{edit?"Editar":"Nuevo"} producto</h2><div className="formgrid">{["name","description","image_url","price","stock"].map(k=><input className="input" key={k} placeholder={k} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>)}</div><br/><button className="primary" onClick={save}>Guardar</button></section><section className="panel"><table className="table"><thead><tr><th>Producto</th><th>Precio</th><th>Stock</th><th/></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.image_url} {p.name}</td><td>${Number(p.price).toFixed(2)}</td><td>{p.stock}</td><td><button className="chip" onClick={()=>{setEdit(p.id);setForm(p)}}>Editar</button></td></tr>)}</tbody></table></section></main>}
